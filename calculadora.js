let _ = require('lodash');
const miCalculadora = {
  add: _.add,
  suma: _.add,
  ceil: _.ceil,
  divide: _.divide,
  floor: _.floor,
  max: _.max,
  maxBy: _.maxBy,
  mean: _.mean,
  meanBy: _.meanBy,
  min: _.min,
  minBy: _.minBy,
  multiply: _.multiply,
  round: _.round,
  subtract: _.subtract,
  sum: _.sum,
  sumBy: _.sumBy,
}

module.exports = miCalculadora;
